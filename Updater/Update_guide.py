import json
import os
def update(id, install, verify):
    print(id, install)
    #with open(`../Backend/Guides/${id}.json`, "r") as file:
     #   guide = json.load(file)

    #with open(`../Backend/Guides/${id}.json`, "w") as file:
     #   json.dump(guide, file, indent = 4)
