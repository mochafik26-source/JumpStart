- name: Run generator
  env:
    GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
  run: |
    echo "=== BEFORE PYTHON ==="
    pwd
    ls -la
    echo "=== RUNNING PYTHON ==="
    python -u "Updater/main.py"
    echo "=== AFTER PYTHON ==="
